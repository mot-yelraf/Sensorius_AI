"""Package marker for persisted host-level system configuration directories.

System settings such as broker, UI, Home Assistant, FarmOS, Astral, and weather
forecast values are materialized beneath this package path in repository-local
runs.
"""
